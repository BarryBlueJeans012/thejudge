FileRunWithProcessBuilder has two methods:
compile(String fileName)
run()

compile() searches for the fileName in $HONE/judge_boy/ and compiles it
run() will only run the file that was compiled with the same instance of the class that was called to compile()



